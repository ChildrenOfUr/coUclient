box: wercker/dart
build:
  steps:
      - script:
          name: pub get
          code: pub get
      - script:
          name: run grind.dart deploy
          code: dart ./grind.dart deploy
deploy:
  steps:
      - lukevivier/gh-pages:
          token: $token
          repo: $repo
          basedir: ./out
